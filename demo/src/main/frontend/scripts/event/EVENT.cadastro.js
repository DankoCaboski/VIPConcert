
    const btSubmit = document.querySelector("input[name='botaoSubmit']");

    if (btSubmit) {
        btSubmit.addEventListener('click', () => {
            btSubmit.style.backgroundColor = 'green';

        });
    }

